# 타자 연습 웹서비스 — 추천 기능 로드맵

> **최종 갱신**: 2026-01-30  
> **역할**: 기능 확장 로드맵의 **단일 진실(SoT)**  
> **기준 스키마**: [../db/SCHEMA.md](../db/SCHEMA.md) 참조

---

## 0) 현재 기준 (완료된 구성)

### Frontend
- ✅ HomePage / PracticePage / ResultPage / HistoryPage
- ✅ API 클라이언트, Layout 컴포넌트
- ✅ PWA manifest

### Backend
- ✅ `users`, `texts`, `sessions` 앱 구성
- ⏳ 실행/Docker 테스트 미완료

### 확장 스키마 (정의서 포함)
- 📋 `stats_user_daily` (일 단위 집계)
- 📋 `leaderboard_snapshot`, `leaderboard_entry` (랭킹)
- 📋 `challenges_daily_challenge`, `challenges_user_challenge` (챌린지)
- 📋 `sessions_typing_event` (선택: 키 입력/오타 이벤트)

---

## 1) 우선순위 로드맵

```
┌─────────────────────────────────────────────────────────────────┐
│                    Phase 1: MVP (바로 체감)                      │
├─────────────────────────────────────────────────────────────────┤
│  1. 목표/루틴 + 스트릭                                          │
│  2. 오타 Top/패턴 분석 + 맞춤 훈련 추천 (기본형)                  │
│  3. 주간 랭킹 (스냅샷 방식)                                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    Phase 2: 재미/바이럴                          │
├─────────────────────────────────────────────────────────────────┤
│  4. 데일리 챌린지                                               │
│  5. 뱃지/레벨 (성취 시스템)                                      │
│  6. 결과 공유 카드 (이미지)                                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    Phase 3: 콘텐츠/운영 확장                     │
├─────────────────────────────────────────────────────────────────┤
│  7. 문장팩 즐겨찾기/최근 사용                                    │
│  8. 유저 문장팩 업로드 + 검수/신고                               │
│  9. 데이터 내보내기 + 관리자 대시보드                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2) 기능 상세 (테이블/화면/API 관점)

### 2.1 목표/루틴 + 스트릭 ⭐ Phase 1

**핵심 가치**: 매일 "조금씩" 하게 만들고, History가 습관/달성으로 변함

| 항목 | 상세 |
|------|------|
| **DB (기존)** | `stats_user_daily` 활용 |
| **DB (추가)** | `goals_user_goal`, `goals_user_streak` (선택) |
| **API** | `GET/POST /api/goals/me`, `GET /api/stats/daily` |
| **UI** | HomePage: 오늘 목표/진행률, 스트릭 뱃지 |
| | HistoryPage: 스트릭 캘린더 |
| **우선순위** | MVP |

---

### 2.2 오타 Top/패턴 분석 + 맞춤 훈련 ⭐ Phase 1

**핵심 가치**: "그냥 치는 것" → "교정/성장" 경험 제공

| 항목 | 상세 |
|------|------|
| **DB (기본형)** | `sessions_typing_session` (결과 기반) |
| **DB (고급형)** | `sessions_typing_event` (opt-in) |
| **구현 전략** | 1단계: ResultPage에서 오타 Top5 (프론트 계산) |
| | 2단계: 서버 분석 (이벤트 저장 유저만) |
| **API** | `GET /api/insights/summary`, `GET /api/training/recommendations` |
| **UI** | ResultPage: 오타 Top5, "추천 훈련 시작" 버튼 |
| | PracticePage: 추천 코스 |
| **우선순위** | MVP (기본형) |

---

### 2.3 주간 랭킹 (스냅샷) ⭐ Phase 1

**핵심 가치**: 경쟁 요소는 유지율을 크게 올림

| 항목 | 상세 |
|------|------|
| **DB** | `leaderboard_snapshot`, `leaderboard_entry` |
| **배치** | 주 1회/일 1회 스냅샷 생성 |
| **API** | `GET /api/leaderboard`, `GET /api/leaderboard/me` |
| **UI** | LeaderboardPage (신규): 주간/일간 탭, 내 순위 강조 |
| **우선순위** | MVP |

> **상세 요구사항**: [../feature/RANKING_FEATURE.md](../feature/RANKING_FEATURE.md)

---

### 2.4 데일리 챌린지 ⭐ Phase 2

**핵심 가치**: "오늘의 문제"는 일일 재방문을 강하게 만듦

| 항목 | 상세 |
|------|------|
| **DB** | `challenges_daily_challenge`, `challenges_user_challenge` |
| **API** | `GET /api/challenges/today`, `POST /api/challenges/today/start` |
| **UI** | HomePage: 오늘의 챌린지 카드 |
| | ResultPage: 챌린지 결과 + 랭킹 |
| **우선순위** | Next |

---

### 2.5 뱃지/레벨 (성취 시스템) ⭐ Phase 2

**핵심 가치**: 스트릭과 함께 쓰면 강력함 (꾸준함/성장 보상)

| 항목 | 상세 |
|------|------|
| **DB (추가)** | `achievements_badge`, `achievements_user_badge` |
| **근거 데이터** | `stats_user_daily`, `sessions_typing_session` |
| **API** | `GET /api/achievements/me`, `GET /api/achievements/catalog` |
| **UI** | Profile/Settings 또는 HomePage에 "최근 획득 뱃지" |
| **우선순위** | Next |

---

### 2.6 결과 공유 카드 ⭐ Phase 2

**핵심 가치**: 공유 → 유입, 기록 자랑 → 참여 유도

| 항목 | 상세 |
|------|------|
| **DB** | 별도 테이블 없음 (선택: `shares_share_log`) |
| **API** | `GET /api/share/card?session_id=...` 또는 프론트 캔버스 |
| **UI** | ResultPage: "공유하기" 버튼 |
| **우선순위** | Next |

---

### 2.7 문장팩 즐겨찾기/최근 사용 ⭐ Phase 3

| 항목 | 상세 |
|------|------|
| **DB (추가)** | `texts_pack_favorite(user_id, pack_id)` |
| **API** | `POST /api/texts/packs/{id}/favorite`, `GET /api/texts/packs?sort=recent` |
| **UI** | HomePage: 최근 사용 팩, 즐겨찾기 |
| **우선순위** | Later |

---

### 2.8 유저 문장팩 업로드 + 검수/신고 ⭐ Phase 3

| 항목 | 상세 |
|------|------|
| **DB (간단안)** | `texts_text_pack`에 `status`, `reported_count` 추가 |
| **DB (정석안)** | `texts_report`, `texts_moderation_log` |
| **API** | `POST /api/texts/packs`, `POST /api/texts/reports` |
| **UI** | 팩 생성/편집 페이지, 신고 버튼 |
| **우선순위** | Later |

---

### 2.9 데이터 내보내기 + 관리자 대시보드 ⭐ Phase 3

| 항목 | 상세 |
|------|------|
| **DB** | `sessions_typing_session`, `stats_user_daily` |
| **API** | `GET /api/export/sessions.csv`, `GET /api/export/stats_daily.csv` |
| **UI** | Settings: "내 데이터 다운로드" |
| | Admin: 기본 통계, 신고 큐, 챌린지 생성 |
| **우선순위** | Later |

---

## 3) 구현 순서 권장

### Step 1: 인프라 안정화
- [ ] Docker Compose로 Postgres + Django + Nginx 실행 확인
- [ ] 세션 저장/조회 정상 동작 확인

### Step 2: MVP 기능
- [ ] `stats_user_daily` 도입 (write-through 또는 배치)
- [ ] Goals (목표/스트릭) + UI 반영
- [ ] Leaderboard Snapshot + LeaderboardPage

### Step 3: Phase 2
- [ ] Daily Challenge
- [ ] 뱃지/레벨 시스템
- [ ] 결과 공유 카드

### Step 4: Phase 3
- [ ] 문장팩 즐겨찾기
- [ ] 유저 업로드 + 검수
- [ ] 데이터 내보내기

### Step 5: 선택
- [ ] TypingEvent 기반 정밀 오타분석 (opt-in + 보관기간 정책)

---

## 4) 추천 앱/모듈 구조

| 앱 | 테이블 | 우선순위 |
|----|--------|----------|
| `stats` | `stats_user_daily` + 통계 API | MVP |
| `goals` | 목표/스트릭 | MVP |
| `leaderboard` | 스냅샷/엔트리 + 배치 | MVP |
| `challenges` | 데일리 챌린지 | Next |
| `achievements` | 뱃지/레벨 | Next |
| `moderation` | 신고/검수 | Later |

---

## 5) 체크리스트 (스키마 기준 확정 포인트)

- [ ] `TypingSession`을 **원천 데이터**로 유지
- [ ] `user_id` NULL 허용 여부 (게스트 지원/기록 이관)
- [ ] 랭킹은 스냅샷 (권장) vs 실시간 (비권장)
- [ ] TypingEvent 저장 정책 (기본 OFF + opt-in + 보관기간)
- [ ] texts soft delete (`is_active`) 정책 확정
- [ ] 핵심 인덱스 `(user_id, started_at DESC)` 적용

---

## 관련 문서

- [프로젝트 마스터](../PROJECT_MASTER.md): 전체 현황
- [DB 스키마](../db/SCHEMA.md): 테이블 정의 (SoT)
- [연습 모드 확장](../feature/PRACTICE_MODE_EXTENSION.md): 모드별 요구사항
- [주간 랭킹](../feature/RANKING_FEATURE.md): 랭킹 기능 요구사항
